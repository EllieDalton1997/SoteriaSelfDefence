const stat = document.getElementById("st1");
const glowbox = document.getElementById("glow")
console.log("Width: " + stat.offsetWidth + "vvv");
console.log("Height: " + stat.offsetHeight + "px");

function grow (event) {
glowbox.style.position = relati
};


stat.addEventListener("click", grow);